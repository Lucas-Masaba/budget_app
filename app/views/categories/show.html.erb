<p style="color: green"><%= notice %></p>

<%= render @category %>

<div>
  <%# <%= link_to "Edit this category", edit_category_path(@category) %> | %>
  <%= link_to "Back to categories", categories_path %>

  <%= button_to "Destroy this category", @category, method: :delete %>
</div>

    <%= link_to "Add transaction",  "/categories/#{@category.id}/transactions/new", class: "btn btn-outline-light  w-75 p-2" %>

<% @category.transactions.each do |test|%>
  <%= test.name %>
<% end %>

<%= @category.transactions.sum(:amount) %>